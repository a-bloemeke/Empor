services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: empor
      POSTGRES_PASSWORD: empor
      POSTGRES_DB: empor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
